services:
  web:
    build:
      context: .
      args:
        VITE_DATA_MODE: ${VITE_DATA_MODE:-demo}
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-}
    ports:
      - "${FRONTEND_PORT:-8080}:80"
    restart: unless-stopped
