# Optional API prefix. Empty = same origin /api (dev proxy forwards to API_TARGET).
VITE_API_BASE_URL=
# demo or api; api never silently falls back to synthetic data.
VITE_DATA_MODE=demo
API_TARGET=http://localhost:8000
