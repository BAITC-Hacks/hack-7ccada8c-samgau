# Runtime data dependencies from participant 2's requirements-data.lock.
# Shared Pydantic dependencies are already pinned in requirements.lock.
# Keep these versions aligned when updating the original data lock.
et-xmlfile==2.0.0
numpy==2.5.3
openpyxl==3.1.5
pandas==2.3.3
python-dateutil==2.9.0.post0
pytz==2026.3.post1
six==1.17.0
tzdata==2026.4
