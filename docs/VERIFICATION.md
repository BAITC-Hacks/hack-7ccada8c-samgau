# Проверка бэкенда 0.2.0

Дата: 23.09.2026. Linux x86_64, Python 3.12.14. Основа — main `92982cb` (последние изменения README/ТЗ включены).

## Выполнено

- `python -m pytest`: **92 passed**. Включены тесты движка, импорта,
  общего API, автоматической регистрации demo-engine-v1 и конфигурации.
- `uv pip check`: все 31 установленный пакет совместимы.
- TypeScript-пример клиента проверен tsc; Python-файлы компилируются.
- Сгенерирован OpenAPI с типами импорта, сохранённого расчёта, сводки,
  сравнения сценариев, ошибок и CSV. `git diff --check` без ошибок.
- Docker Compose собран; API и Caddy healthy.
- HTTP smoke через Caddy на localhost:18080: настоящий qor-mvp-1.0,
  156 → задержка 204 → правка 168 → утверждение → CSV. Проверены оба
  поставщика, 2 бухты по 305 м, неизменяемость заказа, конфликт версии,
  отсутствие доступа без токена и изоляция сессий.
- Отдельно пройден smoke прежней платформенной фикстуры.
- Созданы и загружены 12 синтетических XLSX, по шесть на каждого поставщика.
  Проверены completed, дедупликация, quality, расчёты, приватность и экспорт.
- После перезапуска API сохранённые импортированные наборы, сессия,
  расчёты и утверждённый CSV доступны по прежнему токену.
- Сбой создания папки импорта больше не блокирует следующие загрузки.
- Алгоритмы engine/importers, контракт данных и frontend не изменялись.
  Один тест участника 2 теперь проверяет реальный bootstrap без ручной
  вставки demo-набора в тестовую базу.

Starlette выдаёт предупреждение о будущем переходе TestClient с httpx на
httpx2. На закреплённых версиях тесты проходят; это не сбой runtime API.

## Границы проверки

- Настоящий сетевой вызов ИИ не выполнен: ключ/модель не предоставлены.
  Mock-проверки адаптера и fallback проходят. Для живой проверки есть
  scripts/ai_smoke.py, который не считает fallback успехом.
- 12 синтетических книг не доказывают поддержку реальных коммерческих
  форматов. Нужны реальные файлы и проверка маппинга участником 2.
- Нагрузка около 250 тысяч строк и качество прогноза на реальных данных
  не измерялись; производственная эксплуатация не заявляется.
- Браузерный путь с frontend не проверялся: текущая задача — backend.
  Контракт для следующего этапа: FRONTEND_HANDOFF.md.
- Публичный сервер/HTTPS не развёрнут; Windows/macOS здесь не запускались.
- Шаблон CI сохранён в docs/ci/backend.yml и не активирован. Для публикации
  активного workflow текущей авторизации GitHub не хватает права workflow.
