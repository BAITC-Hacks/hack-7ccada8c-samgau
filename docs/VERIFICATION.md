# Проверка объединённого проекта

Дата: 23.09.2026. Среда: Linux x86_64, Python 3.12.14.

## Выполнено

- `python -m pytest`: **23 passed** (18 проверок платформы и 5 проверок совместимости настроек команды).
- Проверка маршрута заказа: исходная рекомендация 156, ручная правка 168,
  утверждение версии 3, CSV содержит 168 и сохранённый строковый код.
- Перенос поступления за горизонт: новый расчёт 204, исходный 156 сохранён.
- Неизвестный остаток и неправильная кратность блокируют утверждение.
- Изоляция сессий: чужие runs/orders/datasets/imports недоступны.
- Перезапуск: сессии/черновики сохраняются; незавершённый импорт прерывается явно.
- Атомарный CAS: два конкурентных обновления одной версии — ровно одно успешное.
- Подключаемые importer/engine вызываются через контракт, повторный импорт не дублирует набор.
- ИИ-адаптер с MockTransport: валидный JSON принят, неизвестная партия/фактор
  отвергнуты, ошибки дают fallback; объяснение кэшируется.
- CSV защищён от строк, распознаваемых как формулы.
- `uv pip check`: все 31 установленный пакет совместимы.
- Docker Compose собран и запущен в Linux x86_64; API и Caddy healthy.
- HTTP smoke через Caddy на `http://127.0.0.1:18080`:
  `PASS: session -> 156 -> scenario 204 -> edit 168 -> approve -> CSV`.
- Сгенерирован `docs/openapi.json` из фактического приложения.

Тестовая библиотека Starlette выдала предупреждение о будущем переходе TestClient
с httpx на httpx2. На установленных зафиксированных версиях все тесты проходят;
это не ошибка runtime API.

## Не выполнено здесь

- Запуск на настоящей Windows, macOS 11/Apple Silicon и отдельных Ubuntu/Debian.
- Получение публичного HTTPS-сертификата и публикация на сервере.
- Настоящий сетевой вызов OpenAI/NVIDIA: нет предоставленного API-ключа и модели.
- Полный расчёт по 12 Excel: это реализация участника 2, пока не подключена.
- Проверка React-интерфейса: frontend участника 3 пока не подключён.
- Слияние в `main`, публичная ссылка на приложение и сдача проекта.

Шаблон CI находится в `docs/ci/backend.yml` и пока не активирован: текущий
доступ GitHub CLI не имеет права `workflow`. Инструкция — в `docs/ci/README.md`.
